# 땡그랑 프로젝트 작업 지침

## 하드코딩 금지

우리가 만드는 모든 화면/기능은 하드코딩 없이 작성한다.

- 사용자 데이터(닉네임, 이메일, 레벨, 포인트, 코치 톤, 프로필 정보 등)는 항상 Firestore(`UserService`, `watchUser`/`getUser` 등)에서 실시간으로 가져와 표시한다. 화면에 직접 문자열/숫자로 박아 넣지 않는다.
- 이미 실 데이터 소스(컬렉션·서비스)가 존재하는 값은 반드시 그걸 연동해서 쓴다. 절대 화면에 보여줄 값을 임의로 지어내지 않는다.
- 아직 다른 파트(예산·지출·소비심리테스트 등) 백엔드가 준비되지 않아 실 데이터를 못 가져오는 경우에만 예외적으로 더미 데이터를 쓸 수 있다. 이때도:
  - 더미 값은 화면 위젯 안에 흩어 쓰지 말고 파일 상단에 `_mockXxx`처럼 이름 붙은 상수/변수로 한곳에 모아둔다.
  - 사용한 자리마다 `// TODO: ㅇㅇ파트 완성 후 실데이터로 교체` 주석을 남겨서 나중에 실 연동으로 바꿔 끼우기 쉽게 한다.
  - 더미 데이터라는 티가 나지 않게 숨기지 않는다 (예: 실제처럼 보이는 특정 인물 이름, 과도하게 사실적인 금액 등은 지양).
- Firestore에서 오는 옵션 목록(연령대, 직군, 아이콘 매핑 등)도 마찬가지로 `metadata/options` 문서에서 읽어오고, 코드에 리스트를 박아넣는 건 그 문서를 못 읽었을 때의 fallback 용도로만 최소화해서 쓴다.

이 지침은 마이페이지 파트(김은동 담당)뿐 아니라 이 저장소에서 작업하는 모든 화면에 적용된다.
